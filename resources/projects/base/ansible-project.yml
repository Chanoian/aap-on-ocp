apiVersion: tower.ansible.com/v1alpha1
kind: AnsibleProject
metadata:
  name: demo-project          # required
  namespace: aap
spec:
  name: demo-project          
  organization: Default 
  scm_type: git               # git, manual, etc.
  connection_secret: controller-access
