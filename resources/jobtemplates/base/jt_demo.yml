kind: JobTemplate
apiVersion: tower.ansible.com/v1alpha1
metadata:
  name: hello-job
  namespace: aap
spec:
  playbook: "hello.yml"
  inventory: "localhost"    # For testing, use local inventory
  credential: "dummy"      # Create a minimal credential in AAP UI or via resource operator